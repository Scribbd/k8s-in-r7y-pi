---
tags: [k8s, deployment]
---
# Pod vs Deployment
I thought a `Pod` was just K8s way of saying docker-compose deployment. But then I discovered that a `Deployment` is also a thing. And a `Deployment` isn't exactly like a `Pod`. However, `Pod` can be part of a `Deployment`.

